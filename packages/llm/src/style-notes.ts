// Shared label for the style-memory grounding block injected into
// resume-tailor / cover-letter prompts. Exported once so both tasks (and
// their tests) use the exact same wording — see
// docs/superpowers/specs/2026-08-02-phase10-memory-ii-design.md §2.
export const STYLE_NOTES_LABEL =
  "The applicant's standing style preferences (from their own past edits) — follow unless the instruction says otherwise:";
